function showSelect(nm) {
    const slc = document.getElementById("span_op" + nm)
    slc.style.display = "inline-block"
}

function noShowSelect(nm) {
    const slc = document.getElementById("span_op" + nm)
    slc.style.display = "none"
}